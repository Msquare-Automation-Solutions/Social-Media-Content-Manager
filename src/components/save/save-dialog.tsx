"use client";

// Phase 3 builds the full Save dialog here (live thumbnail preview, "Change
// thumbnail", inline add-person / add-platform, category + multi-platform).
// Stubbed for the Phase 2 shell commit.
export function SaveDialog() {
  return null;
}
